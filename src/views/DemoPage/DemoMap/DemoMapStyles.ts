import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles({
    mapDivIcon: {
        '& span': {
            position: 'absolute',
            bottom: '4px',
            left: '0px',
            width: '100%',
            color: 'white',
            fontSize: '9px',
            textAlign: 'center',
        },
    },
});
